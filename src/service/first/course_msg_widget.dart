import 'package:flutter/material.dart';

class CourseMsgWidget extends StatelessWidget {
  const CourseMsgWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        // 请在此处添加控件
        
      ],
    );
  }
}
